<%= form_with(model: embassy, local: true) do |form| %>
  <% if embassy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(embassy.errors.count, "error") %> prohibited this embassy from being saved:</h2>

      <ul>
        <% embassy.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :emb_name %>
    <%= form.text_field :emb_name %>
  </div>

  <div class="field">
    <%= form.label :emb_description %>
    <%= form.text_field :emb_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
